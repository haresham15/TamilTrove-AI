# Optional transformer runtime. The default image uses the deterministic,
# dependency-light multilingual character index.
-r requirements.txt

sentence-transformers==3.0.1
implicit==0.7.2
scipy==1.14.0
wandb>=0.17.0
